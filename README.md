# 세특메이커

학생 활동 키워드를 과목별 세부능력 및 특기사항 초안으로 정리하고 Supabase에 저장하는 웹앱입니다.

## 실행

```bash
npm install
npm run dev
```

## 배포

Vercel과 GitHub 저장소가 연결되어 있어 `main` 브랜치의 새 커밋이 자동으로 배포됩니다.
환경변수는 `.env.example`을 참고하세요. Supabase 테이블은 `supabase/schema.sql`을 SQL Editor에서 실행합니다.

